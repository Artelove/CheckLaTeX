# CheckLaTeX - Инструмент анализа и проверки LaTeX документов

CheckLaTeX - это веб-сервис для статического анализа LaTeX документов, который помогает выявлять типичные ошибки оформления и нарушения стилистических правил.

## Особенности

### 📍 **Точная локализация ошибок**
- Определение файла, строки и колонки для каждой найденной ошибки
- Предоставление контекста (исходный текст с ошибкой)
- Предложения по исправлению там, где это возможно

### 🔧 **Конфигурируемые правила**
- Настройка правил через `lint-rules.json`
- Возможность включения/выключения отдельных проверок
- Поддержка пользовательских настроек стиля

### 🌐 **REST API**
- **POST /api/lint/check** - проверка одиночного документа
- **POST /api/lint/analyze** - анализ проекта из JSON
- **POST /api/lint/analyze-zip** - анализ проекта из ZIP архива
- **POST /api/diagnostic/test** - диагностическая проверка парсера

## Реализованные проверки

1. **Проверка кавычек** - выявление использования неправильных кавычек (' ") с рекомендацией замены на типографские («»)
2. **Проверка дефисов** - замена компьютерного дефиса (-) на тире (—) в соответствующих контекстах
3. **Проверка списков** - анализ описания списков (регистры, последние символы)
4. **Проверка ссылок** - соответствие ссылок (\ref) и меток (\label) в окружениях
5. **Проверка дублирования меток** - выявление повторяющихся \label в документе
6. **Проверка библиографии** - соответствие ссылок (\cite) и библиографических источников
7. **Проверка длины подписей** - контроль длины подписей к рисункам и таблицам

## Структура ответа API

### Формат ошибки
```json
{
  "type": "Error|Warning|Info",
  "info": "Описание ошибки",
  "fileName": "chapter1/main.tex",
  "lineNumber": 42,
  "columnNumber": 15,
  "endLineNumber": 42,
  "endColumnNumber": 25,
  "originalText": "неправильный \"текст\"",
  "suggestedFix": "правильный «текст»",
  "command": "\\cite{source1}"
}
```

### Пример ответа /api/lint/check
```json
{
  "commandsFound": 156,
  "testResults": [
    {
      "testName": "TestQuotationMarks",
      "errors": [
        {
          "type": "Warning",
          "info": "Использование запрещенных кавычек",
          "fileName": "document.tex",
          "lineNumber": 23,
          "columnNumber": 45,
          "originalText": "\"неправильные кавычки\"",
          "suggestedFix": "«правильные кавычки»"
        }
      ]
    }
  ]
}
```

## Использование

### Настройка окружения
1. Установите .NET 6.0 или выше
2. Склонируйте репозиторий
3. Настройте конфигурацию в `lint-rules.json`

### Запуск сервера
```bash
cd tex-lint
dotnet run
```

Сервер запустится на `http://localhost:5000` (или порту из конфигурации).

### Конфигурация правил (lint-rules.json)
```json
{
  "quotationMarks": {
    "preferredOpen": "«",
    "preferredClose": "»",
    "forbidden": ["\"", "'"]
  },
  "hyphen": {
    "wrongSymbol": "-",
    "replacement": "—"
  }
}
```

## Интеграция с VS Code

Проект поддерживает интеграцию с расширением VS Code для анализа LaTeX файлов в режиме реального времени:

1. Расширение отправляет POST-запрос на `/api/lint/check`
2. Сервер возвращает детализированную информацию об ошибках
3. Расширение отображает диагностику прямо в редакторе с точным указанием места ошибки

### Формат запроса из расширения
```json
{
  "content": "\\documentclass{article}...",
  "filePath": "main.tex"
}
```

## Архитектура

### Основные компоненты
- **Controllers/** - Web API контроллеры
- **Models/** - Модели данных и обработка команд LaTeX
- **TestFunctionClasses/** - Классы проверочных функций
- **Models/HandleInfos/** - Конфигурационные сервисы

### Система ошибок
Класс `TestError` поддерживает:
- Типизированные ошибки (Error/Warning/Info)
- Точную локализацию (файл, строка, колонка)
- Контекстную информацию (исходный текст)
- Предложения по исправлению
- Связь с командой LaTeX, вызвавшей ошибку

### Extensible Design
Легко добавлять новые правила проверки:
1. Создать класс, наследующий от `TestFunction`
2. Реализовать логику проверки в конструкторе
3. Добавить функцию в `TestFunctionHandler`
4. Настроить правило в `lint-rules.json`

## Производительность

- Оптимизирован для больших LaTeX документов
- Поддержка обработки ZIP архивов с проектами
- Автоматическая очистка временных файлов
- Retry-логика для освобождения заблокированных файлов

## Диагностика

Для отладки проблем используйте endpoint `/api/diagnostic/test`, который предоставляет детальную информацию о процессе парсинга файлов.

## Разработка

### Требования
- .NET 6.0 SDK
- Понимание структуры LaTeX документов
- Знание паттернов SOLID и Clean Architecture

### Принципы
- **LaTeX-First Design**: Все решения принимаются с учетом специфики LaTeX
- **Configuration-Driven**: Максимальная настраиваемость через конфигурацию
- **Error Localization**: Каждая ошибка должна иметь точное местоположение
- **Graceful Degradation**: Сбой одной проверки не должен останавливать остальные

---

### Благодарности
При использовании CheckLaTeX в своих проектах, пожалуйста, укажите это в документации для поддержки проекта и улучшения качества LaTeX-анализа в сообществе.
