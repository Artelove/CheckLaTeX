\documentclass[bachelor,
               substylefile=VolSU.rtx]{disser}
//

[parameter(with out value), parameter = value]
{arg1, arg2} = {arg1}{arg2}
|все что здесь один аргумент (наверное)|
Запятая - отделитель между разными аргументами/параметрами
Значением аргумента является value после ':' \ref{ls:01}.
Begin всегда должен иметь End со схожими аргументами. 
\begin{}
    
\end{}
\begin{lstlisting}[
    language=Tex, 
    caption={Данный порядок команд вызовает ошибку компиляции}, 
    label={ls:01}
]
Значением аргумента может быть аргумент
Значением параметра может быть аргумент
Значенеем аргумента может быть аргумент со значением.
После TeX Latex можно "\verb| |", " \ ",
после\guillemotright нужен отступ

\guillemotleft можно пробелы
\guillemotright нельзя пробелы
    
Пока не появится символ не относящийся к escape-последовательности
можно ожидать новых открывающихся аргументов или параметров
    
После нахождения всех команд, необходимо проверить их
смысловую наполненность аргументами и параметрами
    
    
    name: "begin"
    paramsCount:1
    optParams:{
        caption
    }
    
    
Расшифровка json-значений

"section":{
    "param":{ "type": "phrase", "count": "?"}, 
    "arg":{ "type": "phrase", "count": "1"},
    "order": ["p","a"]
},
    phrase: Все что написано внутри скобок является значением
    value: Ожидается связка NAME=VALUE, где value может быть null
    
    count: 
        1: Ожидается одно значение
        ?: Ожидается одно значение, либо обозначение параметра\аргумента может отсутвовать  
        0+: обозначение параметра\аргумента может отсутвовать, либо значение может быть сколь угодно много
        1+: Ожидается одно или более значений